[project]
name = "cachyos-forgejo"
version = "0.1.0"
description = "Forgejo setup scripts and utilities"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
]

[tool.hatch.build.targets.wheel]
packages = []
